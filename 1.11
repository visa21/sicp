function f(n){
    return fIter(n,1,3);
}

function fIter(product,counter,max_count){
    return counter > max_count
    ? product + 1
    :fIter(counter*product-counter,counter+1,max_count);
}

f(5);